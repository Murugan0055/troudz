export interface TestimonialType {
    id: number,
    img: string
    ratings: number,
    review: string,
    name: string
}

export interface ServicesType {
    id: number,
    img: string,
    title: string,
    para: string,
    link: string,
}